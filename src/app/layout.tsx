import type { Metadata } from 'next'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'Acestars Tennis Coaching – Book your next Tennis coaching lesson',
  description: 'For over 10 years, Acestars have been providing high quality professional tennis coaching to communities in Berkshire, Hampshire & Surrey.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
